// clone the repo - this will make the folder available in your machine
git clone https://github.com/dheerajayati/Mycodes.git


// to check the status
git status

// to see the branch; your current working branch will be in green color.
git branch

// check the status of ur repo
git status


// Make the changes add into the staging dock
git add <path>
or
git add .

// Commit the changes to create a checkpoint
git commit -m "<What is this commit for; What is this checkpoint for>"

// To push the changes into server; to see in github
git push origin master

dheerajayati